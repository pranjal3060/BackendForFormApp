# Backend Server

This is a simple Express server for handling form submissions.

## Setup

1. Install dependencies:
   ```bash
   npm install

   Start the server:
            npx nodemon

             http://localhost:3000
